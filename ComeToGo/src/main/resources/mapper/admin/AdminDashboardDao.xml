<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cometogo.ctg.admin.dao.AdminDashboardDao">
    <select id="getDashboardStats" resultType="com.cometogo.ctg.admin.dto.AdminDashboardStatsDto">
        SELECT
            (SELECT COUNT(*) FROM ctg_users WHERE join_date >= #{startDate}) AS newUserCount,
            (SELECT COUNT(*) FROM ctg_group) AS groupTotalCount,
            (SELECT COUNT(*) FROM ctg_report WHERE report_status = 'PENDING') AS reportCount
    </select>
</mapper>