<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.cometogo.ctg.group.dao.GroupDao">


    <!-- GroupDetailDto의 속성에 DB 컬럼 값을 연결하는 정의 -->
    <resultMap id="GroupDetailDto" type="com.cometogo.ctg.group.dto.GroupDetailDto">
        <id property="groupId" column="group_id"/>
        <result property="ownerUserId" column="owner_user_id"/>
        <result property="categoryId" column="category_id"/>
        <result property="groupName" column="group_name"/>
        <result property="intro" column="intro"/>
        <result property="joinMessage" column="join_message"/>
        <result property="createdAt" column="created_at"/>
        <result property="categoryName" column="category_name"/>
        <result property="region" column="region"/>
        <result property="isOwner" column="owner"/>
        <result property="isMember" column="member"/>
    </resultMap>

    <!-- MemberDto의 속성에 DB 컬럼 값을 연결하는 정의 -->
    <resultMap id="MemberDto" type="com.cometogo.ctg.group.dto.MemberDto">
        <id property="userId" column="user_id"/>
        <result property="nickname" column="nickname"/>
        <result property="role" column="role"/>
        <result property="joinedAt" column="joined_at"/>
    </resultMap>

    <!-- ScheduleDto의 속성에 DB 컬럼 값을 연결하는 정의 -->
    <resultMap id="ScheduleDto" type="com.cometogo.ctg.group.dto.ScheduleDto">
        <id property="scheduleId" column="schedule_id"/>
        <result property="groupId" column="group_id"/>
        <result property="userId" column="user_id"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="startTime" column="start_time"/>
        <result property="endTime" column="end_time"/>
    </resultMap>

    <!-- PostDto의 속성에 DB 컬럼 값을 연결하는 정의 -->
    <resultMap id="PostDto" type="com.cometogo.ctg.group.dto.PostDto">
        <id property="boardId" column="board_id"/>
        <result property="groupId" column="group_id"/>
        <result property="userId" column="user_id"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="createdAt" column="created_at"/>
        <result property="viewCount" column="view_count"/>
    </resultMap>


    <!-- 🔹 1. 그룹 생성 -->
    <insert id="insertGroup" parameterType="com.cometogo.ctg.group.dto.GroupDto" useGeneratedKeys="true" keyProperty="groupId">
        INSERT INTO ctg_group (
            owner_user_id, category_id, intro, group_name,
            join_message, created_at
        ) VALUES (
                     #{ownerUserId}, #{categoryId}, #{intro}, #{groupName},
                     #{joinMessage}, NOW()
                 )
    </insert>

    <!-- 🔹 2. 그룹 위치 등록 -->
    <insert id="insertGroupLocation" parameterType="com.cometogo.ctg.group.dto.GroupDto">
        INSERT INTO ctg_group_location (
            group_id, city, district, neighborhood
        ) VALUES (
                     #{groupId}, #{city}, #{district}, #{neighborhood}
                 )
    </insert>

    <!-- 🔹 3. 그룹 멤버 등록 -->
    <insert id="insertGroupMember">
        INSERT INTO group_users (
            group_id, user_id, role, status, joined_at
        ) VALUES (
                     #{groupId}, #{userId}, #{role}, #{status}, NOW()
                 )
    </insert>

    <!-- 동호회 이름 중복체크-->
    <select id="isGroupNameExists" parameterType="string" resultType="boolean">
        SELECT EXISTS(
            SELECT 1
            FROM ctg_group
            WHERE group_name= #{groupName}
        )
    </select>

    <!--<select id="getAllCategories" resultType="com.cometogo.ctg.group.dto.CategoryDto">
        SELECT category_id AS categoryId,
               parent_id AS parentId,
               category_name AS categoryName
        FROM ctg_categories
        ORDER BY category_id;
    </select>-->


    <select id="getGroupDetailById" resultType="com.cometogo.ctg.group.dto.GroupDetailDto">
        SELECT
            g.group_id,
            g.group_name,
            g.intro,
            g.join_message,
            g.created_at,
            g.group_status,
            g.owner_user_id,
            g.category_id,
            c.category_name,
            CONCAT(l.city, ' ', l.district, ' ', l.neighborhood) AS region,

            CASE WHEN g.owner_user_id = #{loginUserId} THEN TRUE ELSE FALSE END AS owner,
            CASE WHEN EXISTS (
                SELECT 1
                FROM group_users gu
                WHERE gu.group_id = g.group_id
                  AND gu.user_id = #{loginUserId}
                  AND gu.status = 'ACTIVE'
            ) THEN TRUE ELSE FALSE END AS member

        FROM ctg_group g
                 LEFT JOIN ctg_categories c ON g.category_id = c.category_id
                 LEFT JOIN ctg_group_location l ON g.group_id = l.group_id
        WHERE g.group_id = #{groupId}
    </select>


    <select id="getMemberCountByGroupId" parameterType="long" resultType="int">
        SELECT COUNT(user_id)
        FROM group_users
        WHERE group_id = #{groupId} AND status = 'ACTIVE'
    </select>

    <select id="isUserMemberOfGroup" resultType="boolean">
        SELECT
            CASE WHEN COUNT(*) > 0 THEN TRUE ELSE FALSE END
        FROM group_users
        WHERE group_id = #{groupId}
          AND user_id = #{userId}
    </select>


    <select id="getPostCountByGroupId" parameterType="long" resultType="int">
        SELECT COUNT(board_id)
        FROM group_board
        WHERE group_id = #{groupId}
    </select>


    <select id="getMembersByGroupId" parameterType="long" resultMap="MemberDto">
        SELECT
            gu.user_id,
            cu.nickname ,
            gu.role,
            gu.joined_at
        FROM group_users gu
                 LEFT JOIN ctg_users cu ON gu.user_id = cu.user_id
        WHERE gu.group_id = #{groupId} AND gu.status = 'ACTIVE'
        ORDER BY gu.joined_at ASC
            LIMIT 10
    </select>


    <select id="getScheduleByGroupId" parameterType="long" resultMap="ScheduleDto">
        SELECT
            schedule_id,
            group_id,
            user_id,
            title,
            content,
            start_time,
            end_time
        FROM schedule
        WHERE group_id = #{groupId} AND start_time >= CURRENT_TIMESTAMP
        ORDER BY start_time ASC
            LIMIT 5
    </select>


    <select id="getPostsByGroupId" parameterType="long" resultMap="PostDto">
        SELECT
            board_id,
            group_id,
            user_id,
            title,
            content,
            created_at,
            view_count
        FROM group_board
        WHERE group_id = #{groupId}
        ORDER BY created_at DESC
            LIMIT 5
    </select>



    <select id="selectMyGroup" parameterType="long" resultMap="GroupDetailDto">
        SELECT
            cg.group_id,
            cg.owner_user_id,
            cg.category_id,
            cg.group_name,
            cg.intro,
            cg.join_message,
            cg.created_at,
            cg.profile_image,
            cc.category_name,
            CONCAT(cgl.city, ' ', cgl.district, ' ', cgl.neighborhood) AS region
        FROM group_users gu
                 LEFT JOIN ctg_group cg ON gu.group_id = cg.group_id
                 LEFT JOIN ctg_categories cc ON cg.category_id = cc.category_id
                 LEFT JOIN ctg_group_location cgl ON cg.group_id = cgl.group_id
        WHERE gu.user_id = #{userId} AND gu.status = 'ACTIVE'
    </select>

    <select id="getMyGroups" resultType="com.cometogo.ctg.group.dto.MyGroupDto">
        SELECT
            gj.user_id,
            gj.group_id,
            g.group_name,
            g.category_id,
            gj.join_status AS status,
            gj.created_at AS joinedAt,
            gj.processed_at AS leftAt
        FROM ctg_group_join gj
                 JOIN ctg_group g ON gj.group_id = g.group_id
        WHERE gj.user_id = #{userId}
          AND gj.join_status = 'APPROVED'
        ORDER BY gj.created_at DESC
    </select>

    <select id="selectAllGroups" resultType="com.cometogo.ctg.group.dto.GroupDetailDto">
        SELECT
            g.group_id,
            g.owner_user_id,
            g.category_id,
            g.group_name,
            g.intro,
            g.join_message,
            g.created_at,
            c.category_name,
            CONCAT(gl.city, ' ', gl.district, ' ', gl.neighborhood) AS region
        FROM ctg_group g
                 LEFT JOIN ctg_categories c ON g.category_id = c.category_id
                 LEFT JOIN ctg_group_location gl ON g.group_id = gl.group_id
        WHERE g.group_status = 'ACTIVE'
        ORDER BY g.created_at DESC
    </select>

    <select id="searchGroups" resultType="com.cometogo.ctg.group.dto.GroupDetailDto">
        SELECT
        g.group_id,
        g.group_name,
        g.intro,
        g.created_at,
        g.category_id,
        g.group_status,
        c.category_name,
        CONCAT(
        IFNULL(l.city, ''),
        ' ',
        IFNULL(l.district, ''),
        ' ',
        IFNULL(l.neighborhood, '')
        ) AS region,
        COUNT(gu.user_id) AS memberCount
        FROM ctg_group g
        LEFT JOIN group_users gu ON g.group_id = gu.group_id
        LEFT JOIN ctg_categories c ON g.category_id = c.category_id
        LEFT JOIN ctg_group_location l ON g.group_id = l.group_id
        <where>
            <if test="keyword != null and keyword != ''">
                AND (g.group_name LIKE CONCAT('%', #{keyword}, '%')
                OR g.intro LIKE CONCAT('%', #{keyword}, '%'))
            </if>

            <if test="region != null and region != ''">
                AND (
                l.city LIKE CONCAT('%', #{region}, '%')
                OR l.district LIKE CONCAT('%', #{region}, '%')
                OR l.neighborhood LIKE CONCAT('%', #{region}, '%')
                )
            </if>

            <if test="category != null and category != ''">
                AND g.category_id = #{category}
            </if>
        </where>

        GROUP BY
        g.group_id,
        g.group_name,
        g.intro,
        g.created_at,
        g.category_id,
        g.group_status,
        c.category_name,
        l.city,
        l.district,
        l.neighborhood

        <choose>
            <when test="sort == 'popular'">
                ORDER BY memberCount DESC
            </when>
            <when test="sort == 'latest'">
                ORDER BY g.created_at DESC
            </when>
            <otherwise>
                ORDER BY g.group_id DESC
            </otherwise>
        </choose>
    </select>


</mapper>